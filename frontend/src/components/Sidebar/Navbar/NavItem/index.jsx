import { Button } from '../../../Button';

export const NavItem = (props) => {
    // const { link } = props;
    return (
        // <Link to={link}>
        <Button props={props} />
        // </Link>
    )
}